// A priority queue is an abstract data type similar to a regular queue or stack, but where each element has an associated “priority.” Instead of simple FIFO or LIFO ordering, a priority queue always removes the element with the highest (or lowest) priority first.

// We can use heap to implement priority queue.

#include <bits/stdc++.h>
using namespace std;

int main() {
    // priority_queue<int> pq;  // Max-Heap by default

    // // Insert elements
    // pq.push(10);
    // pq.push(20);
    // pq.push(15);

    // // Print top element (max)
    // cout << "Top element: " << pq.top() << endl;

    // cout << "Size: " << pq.size() << endl;

    // // Remove top element
    // pq.pop();

    // // Print new top element
    // cout << "New top element: " << pq.top() << endl;

    // return 0;

    // priority_queue<int, vector<int>, greater<int>> pq;  // Min-Heap

    // // Insert elements
    // pq.push(10);
    // pq.push(20);
    // pq.push(15);

    // // Print top element (min)
    // cout << "Top element: " << pq.top() << endl;

    // cout << "Size: " << pq.size() << endl;

    // // Remove top element
    // pq.pop();

    // // Print new top element
    // cout << "New top element: " << pq.top() << endl;

    // return 0;
}